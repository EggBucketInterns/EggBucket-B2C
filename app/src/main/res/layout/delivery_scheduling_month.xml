<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="fill_horizontal">
        <!-- Back Arrow -->
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:contentDescription="Back"
            android:padding="4dp"
            android:src="@drawable/baseline_arrow_back_24" />
        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"/>
        <!-- Save Button -->
        <Button
            android:id="@+id/save_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right"
            android:backgroundTint="@color/appThemeColor"
            android:text="Save"
            android:textColor="#fff" />
    </LinearLayout>

    <!-- Repeat Every Text -->
    <TextView
        android:id="@+id/repeat_every_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="4dp"
        android:text="Repeat Every"
        android:textColor="#000"
        android:textSize="16sp" />

    <!-- Repeat Every Options -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="fill_horizontal">

        <com.google.android.material.textfield.TextInputLayout
            android:layout_width="100dp"
            android:layout_height="wrap_content">

            <com.google.android.material.textfield.TextInputEditText
                android:layout_width="100dp"
                android:layout_height="wrap_content"
                android:hint="1" />
        </com.google.android.material.textfield.TextInputLayout>

        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"/>

        <!-- Week Button -->
        <ToggleButton
            android:id="@+id/repeat_week"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:background="@drawable/toggle_button_selector"
            android:textOn="Week"
            android:textOff="Week"
            android:textColor="#000" />

        <!-- Month Button -->
        <ToggleButton
            android:id="@+id/repeat_month"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/toggle_button_selector"
            android:textOn="Month"
            android:textOff="Month"
            android:textColor="#000" />

    </LinearLayout>

    <!-- Repeats On Text -->
    <TextView
        android:id="@+id/repeats_on_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="4dp"
        android:text="Repeats On"
        android:textColor="#000"
        android:textSize="16sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="fill_horizontal">
        <!-- Back Arrow -->
        <!-- Same Date -->
        <ToggleButton
            android:id="@+id/same_day"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="50dp"
            android:layout_marginEnd="16dp"
            android:background="@drawable/toggle_button_selector"
            android:textOn="Same Day"
            android:textOff="Same Day"
            android:textColor="#000" />

        <View
            android:layout_width="20dp"
            android:layout_height="match_parent"
            android:layout_weight="1"/>

        <!-- Same Day -->
        <ToggleButton
            android:id="@+id/same_date"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="50dp"
            android:background="@drawable/toggle_button_selector"
            android:textOn="Same Date"
            android:textOff="Same Date"
            android:textColor="#000" />

        <!-- Save Button -->

    </LinearLayout>


</LinearLayout>
